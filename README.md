# sr-ai-agent

```bash
pipx inject fastapi openai
python -m venv .venv
source .venv/bin/activate
which python
pip install "fastapi[standard]"
pip install "openai"
pip install "apsw"
fastapi run

nohup fastapi run --reload &
```
